{
  "name": "dixiyao-personal-website",
  "version": "2.0.0",
  "description": "Personal academic website",
  "scripts": {
    "build": "node build.js",
    "dev": "npx http-server . -p 8080 -o -c-1 --cors",
    "dev:3000": "npx http-server . -p 3000 -o",
    "deploy": "npm run build"
  },
  "dependencies": {
    "dompurify": "^3.0.6",
    "marked": "^11.1.1",
    "run": "^1.5.0"
  },
  "devDependencies": {
    "http-server": "^14.1.1"
  }
}
